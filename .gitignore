# Node
node_modules/
npm-debug.log*

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Secrets
.env
.env.local

# promptfoo
.promptfoo/

# promptfoo redteam artifacts — both embed the generating account's email
# (comment header / "author" field), regenerate on every run, repo is public
redteam.yaml
reports/redteam-*.json

# pytest-cov
.coverage
.coverage.*

# Raw promptfoo JSON exports — regenerable via scripts/run_eval.py; the
# curated markdown scripts/generate_report.py writes into reports/ (not
# reports/raw/) is the actual historical record (docs/plan.md).
reports/raw/

# redteam_authz.py's opt-in file log (REDTEAM_AUTHZ_LOG_PATH) — a raw
# per-run artifact consumed by generate_redteam_report.py, not a committed record.
logs/*.jsonl

# OS
.DS_Store
Thumbs.db
graphify-out/
.claude/
CLAUDE.md
